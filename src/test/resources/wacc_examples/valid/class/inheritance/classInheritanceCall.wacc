# an inherited class is declared, and attempts to call a function defined in its parent

# Output:
# 15

# Program:

begin
  class c {
    int t
    int f() is
      return 15
    end
  }
  class d extends c {
  }

  d class1 = new d();
  int i = call class1.f();
  println i
end
