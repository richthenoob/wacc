# a simple class is declared, and a nested array in it is assigned and accessed.

# Output:
# 3
# 3

# Program:

begin
  class c {
    int[] a,
    int[] b,
    int[][] c
  }
  c class1 = new c();
  class1.a = [1,2,3];
  class1.b = [3,4];
  class1.c = [a,b] ;
  println c[0][2] ;
  println c[1][0]
end
