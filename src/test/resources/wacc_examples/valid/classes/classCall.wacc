# a simple class is declared, but not called

# Output:
# 10

# Program:

begin
  class c {
    int t,
    char c
    int f() is
        return 10
    end
  }
  c class1 = new c();
  int i = 4;
  i = call class1.f();
  println x
end
