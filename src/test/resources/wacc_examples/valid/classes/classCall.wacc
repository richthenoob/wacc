# a simple class is declared, but not called

# Output:
# #empty#

# Program:

begin
  class c {
    int t,
    char c
    int f() is
        return 0
    end
  }
  c class1 = new c() ;
  int i = 4 ;
  i = call class1.f()
end
